---
import Base from '../layouts/Base.astro';
import { getPosts } from '../lib/posts';
import { SECTIONS } from '../config';
import { formatCnDate } from '../lib/format-date';

const all = await getPosts();
const posts = all.slice(0, 12);
---
<Base>
  <main class="container">
    <div class="feed">
      {posts.map((post, i) => (
        <a class:list={['entry', { featured: i === 0 }]} href={`/${post.id}/`}>
          <div class="meta">{SECTIONS[post.section].name} · {formatCnDate(post.data.date)}</div>
          <h2 class="title">{post.data.title}</h2>
          {post.data.summary && <p class="summary">{post.data.summary}</p>}
        </a>
      ))}
      <a class="more" href="/archives/">全部文章 →</a>
    </div>
  </main>
</Base>

<style>
  .feed { max-width: 720px; padding-top: 8px; }

  .entry {
    display: block; text-decoration: none;
    padding: 32px 0; border-top: 1px solid var(--hair);
  }
  .feed .entry:first-child { border-top: 0; padding-top: 48px; }

  .meta {
    font-family: var(--font-ui); font-size: 12.5px; letter-spacing: 0.06em;
    color: var(--faint);
  }
  .title {
    font-family: var(--font-serif); font-weight: 600; font-size: 21px;
    line-height: 1.5; margin: 12px 0 0; color: var(--ink);
    transition: color 0.18s;
  }
  .summary {
    font-family: var(--font-serif); font-size: 15px; color: var(--dim);
    line-height: 1.9; margin: 10px 0 0; max-width: 40em;
  }
  .entry:hover .title { color: var(--accent); }

  /* 最新一篇作头条：同一结构，仅放大 */
  .entry.featured { padding-top: 56px; }
  .entry.featured .title { font-size: 33px; line-height: 1.32; margin-top: 14px; }
  .entry.featured .summary { font-size: 16px; margin-top: 14px; }

  .more {
    display: inline-block; margin-top: 36px;
    font-family: var(--font-ui); font-size: 13px; letter-spacing: 0.08em;
    color: var(--dim); text-decoration: none;
  }
  .more:hover { color: var(--accent); }

  @media (max-width: 720px) {
    .feed .entry:first-child, .entry.featured { padding-top: 28px; }
    .entry.featured .title { font-size: 27px; }
  }
</style>
